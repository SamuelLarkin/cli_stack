version: "3"

tasks:
  default:
    deps:
      - rename

  rename:
    desc: >
      [rename](https://metacpan.org/dist/File-Rename/view/rename.PL):
      Cpan File::Rename.
    cmd: cpan install File::Rename
